@page "{title=Conway}"
@using Net.fm.Data;
@model Net.fm.Pages.ConwayModel
@{
    ViewData["Title"] = "Conway's Game of Life";
}

<h1>@ViewData["Title"]</h1>

<div class="cell-container" id="conwayContainer" style="grid-template-columns: repeat(@((int)Math.Sqrt(ViewData.Model.cellGrid.cellArray.Length)),1fr);">
    @{int index = 0;}
    @foreach (Cell cell in ViewData.Model.cellGrid.cellArray )
    {
        <div id ="@index" class="cell-item @cell.GetStatusClass()" onclick="toggleCellStatus(id)"></div>
        index++;
    }
</div>

<button class="btn btn-secondary" id="startButton">Start</button>
<button class="btn btn-secondary" id="pauseButton">Pause</button>

<div class="slide-container" >
    <input type="range" min="0" max="900" value="0" class="slider" id="timeInterval" />
    <span>Tick interval: <output id="intervalOutput">1</output>s</span>
</div>

<script src="~/js/signalr/dist/browser/signalr.js"></script>
<script src="~/js/bind.js"></script>
